# 손 제스처 원격 그림판 진행 상황

작성일: 2026-08-26  
현재 브랜치: `dev`

## 1. 현재 결과

Python/OpenCV/MediaPipe 기반 손 제스처 그림판을 로컬 서비스 4개로 통합했다.
휴대폰 카메라는 ngrok HTTPS 주소로 접속하고, 프레임은 로컬 비전 분석과 패턴
판정을 거쳐 OpenCV 캔버스에 반영된다.

테스트 주소 형식:

- 모니터: `https://<ngrok-주소>/`
- 휴대폰: `https://<ngrok-주소>/capture.html?t=<세션토큰>`

실제 주소는 `start_local.ps1 -Ngrok` 실행 시 콘솔에 출력된다. 이 리포지토리는
공개이므로 살아있는 ngrok 주소와 세션 토큰을 문서에 적지 않는다. 주소를 아는
사람은 누구나 촬영 엔드포인트에 연결할 수 있다.

## 2. 서비스 구조

```text
휴대폰 카메라
  -> ngrok HTTPS/WSS
  -> 0-web (127.0.0.1:8000)
  -> 2-vision-analysis (127.0.0.1:8760, 상태 8762)
  -> 3-pattern-command (127.0.0.1:8761)
  -> 1-canvas (127.0.0.1:8770)
  -> 0-web 모니터
```

- `containers/0-web`: 휴대폰 촬영 페이지, PC 모니터, QR, 프레임 중계
- `containers/1-canvas`: DRAW/ERASE/ZOOM 명령을 OpenCV 캔버스에 적용
- `containers/2-vision-analysis`: MediaPipe 손 랜드마크 추출
- `containers/3-pattern-command`: 기존 제스처 규칙으로 명령 판정

A에서 B로 보낼 때 JSON 텍스트 헤더 다음에 raw BGR 바이너리 프레임을 보낸다.
B는 PRD 6.1 랜드마크 JSON을 C로 보내고, C는 세션별 연결을 유지하며 캔버스로
명령을 전달한다.

## 3. 캔버스 기능

- `DRAW`: 검지 끝 좌표를 이어 선을 그림
- `ERASE`: 검지 위치 주변을 지움
- `CLEAR`: 그림과 배율을 모두 초기 상태로 되돌림
- 지우개 위치와 크기를 반투명 원으로 표시
- DRAW 중 손가락 방향을 따라 움직이는 펜 커서 표시
- 휴대폰 세로 측정 영역을 전체 캔버스 범위에 매핑
- `ZOOM_IN`/`ZOOM_OUT` 이벤트가 들어온 순간에만 배율 변경
- 줌아웃해도 그릴 수 없는 빈 영역이 생기지 않도록 처리

## 4. 제스처 규칙 (2026-08-26 개정)

이전의 엄지-검지 핀치 줌과 모드 락 규칙은 폐기했다. 현재 규칙은 편 손가락
개수로만 판정한다. 엄지는 개수에 넣지 않는다.

| 자세 | 명령 |
| --- | --- |
| 주먹 (0개) | `CLEAR` — 주먹 한 번당 한 번만 발생 |
| 1개 (검지) | `DRAW` |
| 2개 (검지+중지) | `ERASE` |
| 3개 (검지+중지+약지) | 좌로 이동하면 `ZOOM_IN`, 우로 이동하면 `ZOOM_OUT` |
| 그 외 | `IDLE` |

- 손가락은 검지 → 중지 → 약지 → 새끼 순서로만 센다. 새끼만 편 자세처럼 순서를
  건너뛴 조합은 명령을 만들지 않는다.
- 각 손가락은 PIP 각도 120도 이상이면 펴짐이며, 최근 5프레임 중 4프레임 이상
  유지돼야 확정한다.
- **모드 락이 없다.** 자세가 바뀌면 그 프레임에 바로 명령이 바뀐다. 주먹은
  다른 명령으로 가는 통로가 아니라 초기화 명령 자체다.
- 안정화 창이 채워지기 전 프레임은 전부 CLOSED로 나오므로, 손이 새로 나타났을
  때 주먹으로 오인해 캔버스를 지우지 않도록 5프레임 워밍업 이후에만 `CLEAR`를
  낸다.

### 줌 이동량 기준

좌우 이동은 **화면 정규화 좌표**로 측정한다. 월드 랜드마크는 손 중심 기준이라
손이 화면을 가로질러도 값이 변하지 않기 때문이다.

```text
ratio = (손목#0.x + 검지MCP#5.x + 새끼MCP#17.x) / 3 / 화면상 손바닥 너비
```

손바닥 너비로 나누므로 카메라와의 거리가 바뀌어도 감도가 같다. `ratio`가 기준점
대비 `ZOOM_MOTION_RATIO`(기본 0.12 손바닥 폭)만큼 움직이면 줌 이벤트 하나를
내고 기준점을 그 자리로 옮긴다. 손을 멈추면 `IDLE`이 되고, 반대로 움직이면
곧바로 반대 방향 줌이 된다.

감도는 `ZOOM_MOTION_RATIO` 환경 변수로 조정한다. 값을 키우면 둔해지고, 줄이면
민감해진다.

## 5. 로컬 실행 방법

최초 한 번 의존성을 설치한다.

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

로컬 PC에서만 실행:

```powershell
.\start_local.ps1
```

ngrok으로 휴대폰까지 연결:

```powershell
.\start_local.ps1 -Ngrok -NgrokPath .\tmp\ngrok\ngrok.exe
```

실행 후 출력되는 `Monitor` 주소를 PC에서 열고 QR을 휴대폰으로 스캔한다.
휴대폰 페이지에서 **카메라 시작**을 누르면 된다.

ngrok 토큰 설정은 사용자 설정 영역에 이미 존재한다. 토큰 값을 소스나 문서에
복사하지 않는다. ngrok 실행 파일은 시스템 설치가 아니라 다음 임시 경로에 있다.

```text
tmp/ngrok/ngrok.exe
```

## 6. 확인된 테스트 결과

로컬 통합 E2E에서 640x480 빈 BGR 프레임을 비전 입력 WebSocket으로 보냈다.
프레임이 비전 -> 패턴 -> 캔버스까지 전달되어 캔버스 세션이 생성되는 것을 확인했다.

- 그림판/제스처/패턴 서비스 테스트: 28개 통과
- 비전 분석 테스트: 43개 통과
- 합계: 71개 통과
- ngrok 공개 모니터: HTTP 200
- ngrok 휴대폰 촬영 페이지: HTTP 200
- ngrok QR 이미지: HTTP 200, `image/png`
- `/api/config`: ngrok 공개 주소 반환 확인
- 현재 로컬 상태: 웹 정상, 비전 정상, 캔버스 정상

테스트 명령:

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests
.\.venv\Scripts\python.exe -m pytest containers\2-vision-analysis\tests -q
```

## 7. Git 상태

통합 결과는 `dev` 브랜치에 푸시되어 있다.

- `d2460bd`: 로컬 원격 그림판 서비스 통합
- `d8346ff`: 원격 `dev` 최신 변경 병합

그 이후 QR을 ngrok 주소로 확실히 연결하는 다음 두 파일은 현재 로컬에서만
수정됐고, 사용자 요청에 따라 아직 커밋하거나 푸시하지 않았다.

- `containers/0-web/web/index.html`
- `start_local.ps1`

`Real-time_Gesture_Remote_Drawing.pdf`는 사용자 로컬 파일이므로 Git에 추가하지 않는다.

## 8. 모니터 화면 기능

- 휴대폰 **전면 카메라**를 사용한다. 촬영 페이지가 보내는 프레임 자체를 좌우
  반전하므로 손을 오른쪽으로 옮기면 펜도 오른쪽으로 간다. 이후 단계(랜드마크,
  `index_tip`, 캔버스)는 추가 반전 없이 그대로 맞는다.
- 원본 화면 위에 C가 판정에 쓴 21개 랜드마크 골격을 겹쳐 그린다. 랜드마크는
  C 출력 패킷 → 캔버스 meta → 모니터 경로로 전달되며 새 소켓을 쓰지 않는다.
- 상태 패널에 명령, 모드, **손가락 개수**, 줌 배율, 추론 시간, 수신 FPS를 낸다.
  수신 FPS는 원본 프레임만 세므로 실제 휴대폰 전송 속도와 같다.
- 캔버스 위 버튼으로 **확대 / 축소 / 초기화 / PNG 저장**을 할 수 있다. 버튼은
  모니터 WebSocket으로 A에 액션을 보내고, A가 캔버스의 `/commands` 소켓으로
  같은 명령을 넣는다. 따라서 휴대폰이 꺼져 있어도 즉시 반영된다.
- **PNG 저장**은 줌 라벨과 커서가 없는 원본 해상도 그림을 저장한다. 파일은
  `data/drawings/`에 남고 브라우저로도 바로 내려받아진다. 저장한 그림은 모니터
  하단 갤러리에서 다시 볼 수 있다 (`GET /api/drawings`).

## 9. 다음 확인 사항

1. 휴대폰에서 QR을 스캔하고 전면 카메라 권한을 허용한다.
2. PC 모니터에서 휴대폰 원본, 랜드마크 골격, 캔버스가 동시에 갱신되는지 본다.
3. 실제 손으로 1개(그리기) → 2개(지우기) → 3개 좌우 이동(줌) → 주먹(초기화)을
   차례로 확인한다.
4. 손을 화면에 새로 넣었을 때 캔버스가 저절로 지워지지 않는지 확인한다.
5. 줌 감도가 손에 맞는지 보고 `ZOOM_MOTION_RATIO`를 조정한다.
6. 모니터 버튼 4개와 저장 갤러리를 확인한다.
7. 실제 수신 FPS, MediaPipe 처리 FPS, 종단 지연을 측정한다.

주의: 이전 로컬 실행이 비정상 종료되면 8000, 8760, 8761, 8762, 8770 포트에
Python 프로세스가 남을 수 있다. 재실행 시 `WinError 10048`이 나오면 해당 포트를
점유한 이 프로젝트의 이전 프로세스만 종료한 뒤 다시 실행한다.
