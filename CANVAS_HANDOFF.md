# 캔버스 연결 단계 인수인계

> **폐기된 문서입니다 (2026-08-26).** 아래의 엄지-검지 핀치 줌과 모드 락 규칙은
> 더 이상 쓰이지 않습니다. 현재 제스처 규칙은 `LOCAL_NGROK_HANDOFF.md` 4장을
> 보세요. 이 문서는 캔버스 연결 당시의 기록으로만 남깁니다.

## 현재 진행 상황

웹캠에서 MediaPipe Hand Landmarker로 한 손의 21개 랜드마크를 추출하고,
Container C가 이를 명령으로 판정하는 프로토타입을 만들었다.

- 실행 파일: `app.py`
- 제스처 판정기: `containers/pattern_command/gesture_classifier.py`
- 손가락 펴짐 판정: `containers/pattern_command/index_finger.py`
- 자동 테스트: `tests/` (현재 13개 통과)
- 현재 앱은 웹캠 화면, 랜드마크, 명령 상태, `CAPTURE` 저장 기능을 제공한다.

실행 명령:

```powershell
.\.venv\Scripts\python.exe app.py --output captures\gesture_test
```

## 현재 제스처 규칙

손가락 상태는 각 손가락의 PIP 관절 각도로 판정한다. 120도 이상이면 펴짐으로
간주하며, 최근 5프레임 중 4프레임 이상 같은 결과일 때 확정한다. 거리 값은 카메라
거리 변화에 덜 흔들리도록 3D 월드 좌표와 손바닥 너비로 정규화한다.

| 자세 | 명령 | 비고 |
| --- | --- | --- |
| 검지만 폄 | `DRAW` | 중지·약지·새끼는 접혀야 함 |
| 검지+중지 폄 | `ERASE` | 약지·새끼는 접혀야 함 |
| 엄지 활성 + 검지만 폄 | 줌 시작 후보 | 3프레임 확인 후 줌 락 |

명령 모드는 시작되면 잠긴다. 검지가 3프레임 연속 접혀야 `IDLE`로 해제된다.
따라서 진행 중에 다른 손가락 자세가 잠깐 잡혀도 `DRAW`, `ERASE`, `ZOOM`이 서로
교차하지 않는다.

## 줌 규칙 (현재 핵심)

거리 기준은 다음과 같다.

```text
spacing = 거리(엄지 끝 #4, 검지 끝 #8) / 거리(검지 MCP #5, 새끼 MCP #17)
```

1. 엄지 활성 상태를 3프레임 유지한다.
2. **줌 락이 시작되는 그 순간**의 `spacing`으로 방향을 하나만 고정한다.
   - `spacing <= 0.80`: `ZOOM_IN` 락
   - `spacing >= 1.00`: `ZOOM_OUT` 락
   - `0.80 < spacing < 1.00`: 중립 구간, 줌 락을 만들지 않음
3. 락 이후에는 간격이 0.05 이상 실제로 움직일 때만 명령을 보낸다.
   - `ZOOM_IN` 락: 간격이 더 커질 때만 `ZOOM_IN`
   - `ZOOM_OUT` 락: 간격이 더 작아질 때만 `ZOOM_OUT`
   - 반대로 움직이면 `IDLE`; 반대 줌 명령으로 바뀌지 않음
4. 의미 있는 움직임을 처리한 뒤 그 간격을 새 기준점으로 삼는다. 손을 멈추면
   `IDLE`이므로 줌이 계속 진행되지 않는다.
5. 검지를 3프레임 접으면 줌 락을 해제한다.

이 구조의 목적은 줌인과 줌아웃이 프레임마다 번갈아 발생하거나, 줌 중 `DRAW`로
빠지는 문제를 막는 것이다.

## 다음 작업: 캔버스 연결

목표는 웹캠 판정 결과를 실제 그림판 동작으로 연결하고, 실제 사용 감도를 조정하는
것이다.

권장 구현 순서:

1. OpenCV 창에 흰색 캔버스를 만들고 별도 레이어에 선을 저장한다.
2. `DRAW`일 때 검지 끝 화면 좌표(`#8`)를 이전 좌표와 이어 선을 그린다.
3. `ERASE`일 때 같은 좌표 주변을 배경색으로 지우거나 마스크 방식으로 지운다.
4. `ZOOM_IN`/`ZOOM_OUT` 이벤트마다 캔버스 확대 배율을 일정량 변경한다.
   처음에는 이벤트 한 번당 `1.05배` / `1 / 1.05배` 정도로 시작한다.
5. 줌의 기준점은 검지 끝 위치 또는 캔버스 중앙 중 하나로 정한다. 처음 구현은
   캔버스 중앙 기준이 간단하고 안정적이다.
6. 실제 웹캠으로 아래 값을 조절한다.
   - 줌 시작 경계: `0.80`, `1.00`
   - 움직임 감도: `0.05`
   - 줌 배율: 이벤트당 `1.05배`
   - 손 떨림 방지용 최소 이동 거리

## 주의할 점

- 줌 감도는 코드상 정답이 아니라 실제 캔버스에서 확인하며 보정해야 한다.
- 현재 `app.py`는 명령 판정과 캡처용 프로토타입이다. 캔버스 구현 시에는 이 파일을
  확장하거나 `canvas_app.py`를 새로 만들어도 된다.
- 줌 처리에서 이미지 전체를 매 프레임 확대하지 말고, `ZOOM_IN`/`ZOOM_OUT` 명령이
  발생한 순간에만 배율을 갱신해야 성능과 조작감이 안정적이다.

## 새 컨텍스트에 전달할 프롬프트

```text
나는 Python/OpenCV/MediaPipe 기반 손 제스처 원격 그림판을 만들고 있다.
현재 폴더의 CANVAS_HANDOFF.md를 먼저 읽고, 구현된 제스처 규칙을 절대 임의로
바꾸지 말고 캔버스 연결 작업을 이어서 진행해줘.

목표는 app.py의 MediaPipe 웹캠 판정 결과를 실제 OpenCV 그림판에 연결하는 것이다.
DRAW는 검지 끝 좌표로 선을 그리고, ERASE는 해당 위치를 지우며,
ZOOM_IN/ZOOM_OUT은 이벤트가 발생할 때만 캔버스 배율을 바꿔줘.

줌 규칙은 반드시 유지한다:
- 줌 락 시작 시 간격으로 ZOOM_IN 또는 ZOOM_OUT 방향을 하나로 고정한다.
- 락 이후에는 고정된 방향으로 실제 간격이 움직일 때만 명령을 낸다.
- 반대로 움직여도 반대 줌으로 바꾸지 않는다.
- 검지를 3프레임 접을 때만 줌 락이 해제된다.

먼저 현재 코드를 읽고, 캔버스 구조와 줌 배율 처리 방식을 짧게 설명한 뒤 구현해줘.
기존 제스처 테스트가 깨지지 않게 테스트도 실행해줘. 설명은 한국어로 쉽게 해줘.
```
